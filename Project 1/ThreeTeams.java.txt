package assignment3_000924316;
/**
 * Implementation of a measuring Scale with a random weight on the
 * left side. The user can manipulate the right to try and balance it.
 * @author vanshul vanshul
 */

import javafx.application.Application;
import javafx.scene.Group;
import javafx.scene.Scene;
import javafx.scene.canvas.Canvas;
import javafx.scene.canvas.GraphicsContext;
import javafx.scene.paint.Color;
import javafx.stage.Stage;

import java.util.Random;

import static javafx.application.Application.launch;

/**
 * Use this template to create drawings in FX. Change the name of the class and
 * put your own name as author below. Change the size of the canvas and the
 * window title where marked and add your drawing code where marked.
 *
 * @author YOUR NAME
 */
public class ThreeTeams<Team> extends Application {

    /**
     * Start method (use this instead of main).
     *
     * @param stage The FX stage to draw on
     * @throws Exception
     */
    @Override
    public void start(Stage stage) throws Exception {
        Group root = new Group();
        Scene scene = new Scene(root);
        Canvas canvas = new Canvas(600, 600); // Set canvas Size in Pixels
        stage.setTitle("Robot Olympics"); // Set window title
        root.getChildren().add(canvas);
        stage.setScene(scene);
        GraphicsContext gc = canvas.getGraphicsContext2D();
        gc.setFill(Color.GAINSBORO);
        gc.fillRect(0, 0,600,600);

        // YOUR CODE STARTS HERE

        Random rand=new Random();
        String[] teams=new String[3];
        teams[0]="Warrior";
        teams[1]="Bulls";
        teams[2]="Pirates";
        Color c1,c2,c3;

        c1=Color.color(Math.random(), Math.random(), Math.random());
        c2=Color.color(Math.random(), Math.random(), Math.random());
        c3=Color.color(Math.random(), Math.random(), Math.random());
        Teams team1,team2,team3;

        if(rand.nextInt(3)==0){
            team1 = new Teams(50,30,teams[0],c1);
            team2 = new Teams(50,200,teams[1],c2);
            team3 = new Teams(50,400,teams[2],c3);
        }else if(rand.nextInt(3)==1){
            team1 = new Teams(50,30,teams[1],c2);
            team2 = new Teams(50,200,teams[0],c1);
            team3 = new Teams(50,400,teams[2],c3);
        }else{
            team1 = new Teams(50,30,teams[2],c3);
            team2 = new Teams(50,200,teams[1],c2);
            team3 = new Teams(50,400,teams[0],c1);
        }
        team1.draw(gc);
        team2.draw(gc);
        team3.draw(gc);

        Players ref;
        int refTeam=rand.nextInt(3);
        if(refTeam==0){
            ref=new Players(450,200,c1,"Head "+teams[0]);
        }else if(refTeam==1){
            ref=new Players(450,200,c2,"Head "+teams[1]);
        }else{
            ref=new Players(450,200,c3,"Head "+teams[2]);
        }
        //drawing refree
        ref.draw(gc);


        stage.show();


    }

        
        // YOUR CODE STOPS HERE



    /**
     * The actual main method that launches the app.
     *
     * @param args unused
     */
    public static void main(String[] args) {
        launch(args);
    }


}




